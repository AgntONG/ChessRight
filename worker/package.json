{
  "name": "chessright-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute chessright --file=./schema.sql"
  },
  "dependencies": {
    "chess.js": "^1.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
